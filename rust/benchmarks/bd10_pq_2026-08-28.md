# Native 10-bit input, PQ-shaped code values — 2026-08-28

Host: macOS 25.5.0, aarch64 (Apple silicon), clang. Commit: see `git log` for
the commit that added this file. C oracle: `reference/svt-av1` @ 3115c0c1b
(v4.2.0 + 1), built by the cargo-driven `cbuild-static` path, `SVT_HDR_MODE=0`.

Source mode: `SVTAV1_HBD_SRC=1 SVTAV1_HBD_PQ=1` — 8-bit luma linearized as
sRGB, mapped to a 1000-nit display, SMPTE ST 2084 (PQ) OETF, quantized to
10-bit limited range (64..940); chroma rescaled 8-bit limited (16..240) ->
10-bit limited (64..960). Both encoders read the SAME 16-bit-LE `.yuv`.

## `tools/bd10_hbd_src_gate.sh` (synthetic; the CI-wired gate)

    bd10 NATIVE-10-bit-source identity: 115 / 115 byte-identical

115 = 100 chunk-2 cells (synthetic low-bit pattern) + 15 new PQ cells that
passed unpinned, with 3 aarch64-scoped pins on the chunk-2 side. The PQ tier
is `{64,128}` x qp `{8,20,32}` x preset `{6,8,9}` = 18 cells, **18/18**.

## `tools/bd10_hbd_pq_gate.sh` (real photographic; NOT in CI — no runner corpora)

    bd10 PQ-10bit PHOTOGRAPHIC identity: 48 / 48 byte-identical
    per-ISA pinned (aarch64 only): 12 cells

Corpus CID22-512, images {1001682, 2119713, 4666751, 2738653, 7062227} x
qp {8, 20, 32, 55} x preset {6, 8, 9} = 60 cells.

| preset | result |
|---|---|
| 9 | 20 / 20 byte-identical |
| 8 | 20 / 20 byte-identical |
| 6 | 8 / 20 byte-identical, 12 aarch64-pinned |

Pinned cells and their byte counts (port vs C-on-aarch64):

| cell | port | C |
|---|---|---|
| 1001682_q8_p6 | 55007 | 55019 |
| 1001682_q20_p6 | 20063 | 20059 |
| 2119713_q8_p6 | 59777 | 59967 |
| 2119713_q20_p6 | 24605 | 24599 |
| 4666751_q8_p6 | 32019 | 32084 |
| 4666751_q20_p6 | 11171 | 11123 |
| 4666751_q32_p6 | 4623 | 4625 |
| 2738653_q8_p6 | 91133 | 91135 |
| 2738653_q20_p6 | 34053 | 34107 |
| 7062227_q8_p6 | 41313 | 41266 |
| 7062227_q20_p6 | 18809 | 18812 |
| 7062227_q32_p6 | 9690 | 9712 |

q55 matches on every image; the divergence is the low-qp end.

## Context for the pins — the C oracle is host-dependent at bd10

Same commit, same port binary, `tier_invariance.rs` green:

| gate | CI x86-64 | local aarch64 |
|---|---|---|
| `bd10_matrix.sh` | 36/36 | 36/36 |
| `bd10_hbd_src_gate.sh` | 100/100 | 97/100 |
| `bd10_nonflat_gate.sh` | 309/309 | 197/309 |
| `bd10_photo_gate.sh` (not in CI) | — | 53/191 |

Three failing photographic cells (`1484678 q12 p10`, `1001682 q12 p7`,
`1001682 q12 p5`) were re-encoded by a build of the PRE-SESSION tree
(`bfae1b69`) in a sibling jj workspace: **byte-identical port output**, still
differing from local C. So the divergence tracks the C build, not the port.
Full record: `docs/SUSPECTED-C-BUGS.md` #9.
