# Comprehensive 8-bit byte-parity sweep — port vs the real C encoder
Date:    2026-08-03
Host:    darwin arm64 (Apple); C oracle = in-tree SVT-AV1 v4.2.0, Bin/Release
Tool:    tools/identity_full_8bit.sh (default tiers: synthetic + dims)
Files:   identity_full_8bit_2026-08-03.tsv        default gate run (this file)
         identity_full_8bit_dims_2026-08-03.tsv   dims tier WITH p0/p4 added

## Default gate run: 796/796 byte-identical, +4 pinned, 0 harness errors (exit 0)

  synthetic  4 content {uniform,gradient,diag,screen} x {64,128}px
             x qp {5,20,32,48,63} x EVERY preset 0..13   = 560 cells
  dims       15 geometries (32x32 .. 512x512, incl. odd 65x65, straddle
             80x88, both-partial 120x104) x 2 content x qp {20,48}
             x preset {6,7,9,13}                          = 240 cells

Every preset 0..13 is byte-identical on 64-aligned content at every swept qp,
including q5 and q63. C clamps all-intra presets above M9 to M9
(enc_handle.c:4415-4419) but the PORT does not, so 10..13 were swept as
distinct configurations rather than assumed equal to M9.

## The M9 clamp, MEASURED rather than read

For all 40 (content, size, qp) combinations in the synthetic tier, C's presets
9, 10, 11, 12 and 13 produce byte-IDENTICAL streams — an independent
confirmation of the all-intra clamp at enc_handle.c:4415-4419, from the oracle's
own output rather than from reading its source. Since the port is
byte-identical to C at every one of those presets, the port's p10..p13 output
equals C's M9 output too, even though the port does not implement the clamp.
That is why sweeping 10..13 is worth the cells: it verifies the equivalence
instead of assuming it.

## The 2 pinned cells (self-promoting)

  screen 64x64   q63 p1   C=64B  port=71B
  screen 128x128 q63 p1   C=185B port=193B

The smallest known reproducer of the #71 palette-decision class. Isolated:
p0 and p2 at the same qp are IDENTICAL, and q48/q55 are identical at EVERY
preset. Probe with palette forced off: C=64B port=60B — the port goes SMALLER
than C, so C codes palette here too and the two simply decide differently.

## dims tier WITH the unclaimed band (IF_PRESETS="0 4"): 244/300

  p6/p9/p13  60/60 each — every geometry, every qp
  p0 30/60, p4 34/60, splitting into two already-known classes:
    53  partial-SB at p0/p4 — presets 0-5 skip the C-faithful PD1 walk on a
        non-64-aligned SB (pipeline.rs `refined` requires full_sb), so the
        search is structurally different. docs/arbitrary-dims-port-map.md.
    3   ALIGNED, all `screen` at 256/384/512 — the #71 screen-content RD
        class, same as the production corpus sees on its M0 screen classes.
  No unexplained class.

## Two panics this sweep found and fixed (crates/svtav1-encoder/src/intrabc_hash.rs)

C computes `x_end = pic_width - block_size + 1` as a SIGNED int
(hash_motion.c:195-196, :222-223); negative means the loop body never runs. The
port used `usize`, so it underflowed and wrapped to ~2^64. On a 32x32 screen
frame at preset 0, through the PUBLIC encode API:
  generate_block_hash_value              index out of bounds: len 1024, index 1024
  add_to_hash_map_by_row_with_precal     index out of bounds: len 1024, index 2048
No earlier gate encoded anything below 60x60 with the screen-content tools armed.

## REAL-CORPUS TIER (IF_TIER=real, IF_REAL_N=6): 403/450, 0 harness errors
File: identity_full_8bit_real_2026-08-03.tsv
18 images x 512x512 centre crop x qp {5,20,32,48,63} x preset {0,4,6,10,13}

                p0      p4      p6      p10     p13
  gb82-photo    30/30   30/30   30/30   30/30   30/30
  CID22-photo   26/30   26/30   30/30   30/30   30/30
  gb82-screen   10/30   11/30   30/30   30/30   30/30

**At preset 6 and above the port is byte-identical to C on EVERY real image
tested — photographic and screen, at every qp from 5 to 63.** The entire gap is
presets 0 and 4, and it is the two already-known classes:

  - gb82-screen p0/p4 (39 cells) — the #71 palette/IBC calibration class, the
    same one tools/screen_ibc_gate.sh pins. codec_wiki and gmessages are 25/25
    (they are the non-sc_class5 controls); graph, gui, imac_dark and imac_g3
    diverge by roughly +-1%.
  - CID22 1028637.png (8 cells) — a photo RD near-tie band, +-0.4%, at p0/p4
    only. The other five CID22 images and all six gb82 photos are 25/25.

Magnitudes are small and BOTH directions (the port is smaller than C about as
often as larger), which is the signature of a near-tie rather than a systematic
mis-cost.

## PER-PRESET COVERAGE AUDIT (2026-08-03) — the p1/p2/p3/p7 question

The first sweeps covered presets 0..13 on 64/128px SYNTHETIC content, but the
dims and real tiers ran only {0,4,6,9|10,13}. So p1, p2, p3 and p7 existed in
exactly one tier. That matters because of WHICH feature each preset uniquely
turns on.

### The unique axis is IntraBC, and synthetic content cannot reach it

`intrabc_level` by preset (sc_detect.rs, C enc_mode_config.c:2374-2390):
p0->3, p1->4, p2->5, p3->6, p4->7, p5+ ->0. So p1/p2/p3 are the ONLY presets
carrying levels 4/5/6.

MEASURED IntraBC blocks actually coded (SVTAV1_PACKTREE, q20):

  content                    p0    p1    p2    p3    p4    p7
  synthetic `screen` 128x128   0     0     0     0     0     0
  real screen (terminal) 512  674   588   792   890   558     0

**IntraBC never wins a block on synthetic content at any preset.** Palette does
(92-130 leaves at 128px), so the synthetic tier covers the palette half of the
screen vertical and NONE of the IntraBC half. With the real tier skipping
p1/p2/p3, levels 4/5/6 had no byte-parity coverage anywhere. p7 correctly codes
zero IntraBC (level 0 above p4) — its unique combination is instead
palette_level 7 + the CDEF qp-strength arm + screen detection still live, which
is the intersection tools/screen_palette_bd_gate.sh now covers with 12 p7 cells.

CAUTION for whoever measures this next: the SVTAV1_PACKTREE dump APPENDS. A
first pass at the table above did not remove the file per run and reported
cumulative counts (p7 appeared to code 3502 IntraBC blocks). Remove it per run.

### Coverage added, and what it found

identity_full_8bit_real_p1237_2026-08-03.tsv  (3 images x 3 corpora x 5 qp)
                  p1      p2      p3      p7
  CID22-photo    11/15   11/15   11/15   15/15
  gb82-photo     15/15   15/15   15/15   15/15
  gb82-screen    11/15   10/15   11/15   15/15      -> 155/180

identity_full_8bit_dims_p1237_2026-08-03.tsv  (15 geometries x 2 content x 2 qp)
              aligned   partial
  p1          22/24     7/36
  p2          22/24     10/36
  p3          21/24     11/36
  p7          24/24     34/36                       -> 151/240

**p7 is essentially production-clean**: 45/45 on real content across all three
corpora, 24/24 on aligned geometry, 34/36 on partial. Its two misses are
`screen` q48 at 72x88 and 80x88 — now pinned, and p7 is in the dims gate.

p1/p2/p3 divergences land in exactly the classes p0/p4 already showed, with NO
new class and NO new image: every real-content miss is `1028637.png` (the CID22
RD near-tie band) or `graph.png` (the #71 screen band); every ALIGNED dims miss
is `screen` at q48 on 256/384/512; the partial-dims misses are the documented
presets-0..5 partial-SB structural gap. Magnitudes -1.6%..+1.5%, both
directions.

## COMPLETE PER-PRESET COVERAGE (2026-08-03) — every axis populated

8-BIT COVERAGE MATRIX — 2735 cells across 7 sweeps

           synth        dims-aligned    dims-partial     real-photo     real-screen   
  p0        40/40           23/24            7/36           56/60           10/30      
  p1        40/40           22/24            7/36           26/30           11/15      
  p2        40/40           22/24           10/36           26/30           10/15      
  p3        40/40           21/24           11/36           26/30           11/15      
  p4        40/40           22/24           12/36           56/60           11/30      
  p5        40/40           24/24           25/36           30/30           15/15      
  p6       100/100          24/24           36/36           60/60           30/30      
  p7       100/100          24/24           34/36           30/30           15/15      
  p8       100/100          24/24           36/36           30/30           15/15      
  p9       100/100          24/24           36/36           30/30           15/15      
  p10      100/100          24/24           36/36           60/60           30/30      
  p11      100/100          24/24           36/36           30/30           15/15      
  p12      100/100          24/24           36/36           30/30           15/15      
  p13      100/100          24/24           36/36           60/60           30/30      

  '--' = NO CELLS. An untested axis is a coverage claim nobody checked;
  read these before reading any pass rate.

Regenerate with `python3 tools/coverage_matrix.py` (add --csv for a machine
form). It consolidates every benchmarks/identity_full_8bit*.tsv and prints
`--` for an axis with NO cells, because a missing cell count is a coverage
claim nobody tested and that is more dangerous than a failing one.

### What the completed matrix establishes

- **Presets 5 and 8..13 are byte-identical to C on REAL content — photo AND
  screen, 225/225 across p5/p8/p9/p11/p12** (the p6/p7/p10/p13 columns were
  already clean). Together with p6/p7 that makes **every preset >= 5
  production-clean on real content**.
- **Every preset >= 6 is 24/24 on aligned geometry and 34-36/36 on partial**;
  p8/p10/p11/p12 are a clean 36/36. The dims gate default is therefore widened
  from {6,9,13} to ALL of 6..13 — measured per preset, not assumed to follow a
  neighbour.
- **The remaining gap is presets 0..4 only**, and it is the two known classes:
  partial-SB geometry (7-12/36; presets 0-5 skip the C-faithful PD1 walk on a
  non-aligned SB) and screen/photo RD near-ties on two specific images.
- **p5 is the transition**: 24/24 aligned, 25/36 partial (vs 7-12/36 for p0-p4)
  and 45/45 on real content. Finer-grained than the docs' "presets 0-5" band.

Default gate run after widening: **1036/1036 + 4 pinned, 0 harness errors.**
