date: 2026-07-13
tool: tools/identity_matrix.sh (bitstream-identity differ per cell)
matrix: {uniform,gradient} x {64,128} x cli_qp{20,40,55} x preset{13,10,6} = 36
c_reference: SvtAv1EncApp v4.2.0-rc static, matched still/AVIF CQP --lp 1
commit: eab9d8860 (p6 allintra tool bits: SH filter_intra/restoration + FH
  lr_params + per-block use_filter_intra + CDEF all-skip search outcome)
result: 18/36 byte-identical (was 12/36 at 6da9f3e23)
  identical set: uniform content at EVERY preset (13, 10 AND 6), all qp/sizes —
    the entire SH stage (12 cells at 6da9f3e23) is eliminated
  divergence stages (first-diverging): tile-op=11 (gradient partition/mode
    decision RDO, all presets), FH=7 (gradient128 M10/M13 cdef_uv_sec_strength
    fast-path vs C search [6 cells, pre-existing]; gradient64 q55 M6
    cdef_y_pri 0 vs 10 — C's search result on non-all-skip content vs our qp
    fast path [1 cell, the narrowed gap 2a])
next-fix leverage: mode-decision RDO parity (tile-op, 11 cells) and the
  full CDEF RDO search for non-all-skip frames (FH, 7 cells). The search's
  sb_count==0 outcome is already C-exact (uniform cells prove it); the mse
  path over live filter blocks is the remaining port.
