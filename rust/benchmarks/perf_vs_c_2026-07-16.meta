date: 2026-07-16
command: cargo run --release -p svtav1 --example perf_vs_c
settings: --lp 1, still-picture, CQP q40, median of 3, no target-cpu=native
commit: d61461068
vs 2026-07-13 baseline: p6 19.06x->1.25x (256) / 64.1x->4.29x (1024); p10 256 now 0.87x (BEATS C); p2 regressed 9.7x->14.5x at 1024 (correctness machinery added; no SIMD yet)
gate: <=1.20x — only 256-p10 passes; ratios worsen with size => per-SB overhead scaling suspect (per-SB MdRates/CoeffCostTables rebuilds) on top of missing SIMD
