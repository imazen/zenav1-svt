[package]
name = "svtav1-target"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "zensim HDR target loop for the zenav1-svt still encoder (wave: benchmarks/zensim_hdr_target_wave_2026-08-27.md)"

[dependencies]
# The encoder facade only — the judge is INJECTED by the harness (wave md
# chunk-2 final shape), so this crate carries ZERO metric dependencies.
zenav1-svt = { path = "../../svtav1", package = "zenav1-svt" }
